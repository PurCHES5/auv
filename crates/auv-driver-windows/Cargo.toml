[package]
name = "auv-driver-windows"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[features]
default = []
# TODO(driver-overlay-windows): add the Windows adapter after its native window
# implementation is approved; this feature currently exposes no renderer.
overlay = []

[dependencies]
auv-driver-common = { path = "../auv-driver-common" }
image.workspace = true
thiserror.workspace = true

[target.'cfg(target_os = "windows")'.dependencies]
windows.workspace = true
xcap.workspace = true
